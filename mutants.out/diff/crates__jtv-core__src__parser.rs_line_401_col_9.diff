--- crates/jtv-core/src/parser.rs
+++ delete match arm Rule::integer in parse_number
@@ -393,23 +393,17 @@
             "Unexpected factor: {:?}",
             pair.as_rule()
         ))),
     }
 }
 
 fn parse_number(pair: pest::iterators::Pair<Rule>) -> Result<Number> {
     match pair.as_rule() {
-        Rule::integer => {
-            let n = pair
-                .as_str()
-                .parse::<i64>()
-                .map_err(|e| JtvError::ParseError(format!("Invalid integer: {}", e)))?;
-            Ok(Number::Int(n))
-        }
+         /* ~ changed by cargo-mutants ~ */
         Rule::float => {
             let n = pair
                 .as_str()
                 .parse::<f64>()
                 .map_err(|e| JtvError::ParseError(format!("Invalid float: {}", e)))?;
             Ok(Number::Float(n))
         }
         Rule::rational => {
