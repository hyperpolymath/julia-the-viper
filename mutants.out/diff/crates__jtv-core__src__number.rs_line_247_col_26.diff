--- crates/jtv-core/src/number.rs
+++ replace > with == in <impl fmt::Display for Value>::fmt
@@ -239,17 +239,17 @@
             Value::Hex(n) => write!(f, "0x{:x}", n),
             Value::Binary(n) => write!(f, "0b{:b}", n),
             Value::Symbolic(s) => write!(f, "{}", s),
             Value::Bool(b) => write!(f, "{}", b),
             Value::String(s) => write!(f, "\"{}\"", s),
             Value::List(l) => {
                 write!(f, "[")?;
                 for (i, v) in l.iter().enumerate() {
-                    if i > 0 {
+                    if i == /* ~ changed by cargo-mutants ~ */ 0 {
                         write!(f, ", ")?;
                     }
                     write!(f, "{}", v)?;
                 }
                 write!(f, "]")
             }
             Value::Tuple(t) => {
                 write!(f, "(")?;
