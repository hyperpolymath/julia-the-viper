--- crates/jtv-core/src/interpreter.rs
+++ replace Interpreter::eval_top_level -> Result<()> with Ok(())
@@ -129,17 +129,17 @@
         self.trace.clear();
         for statement in &program.statements {
             self.eval_top_level(statement)?;
         }
         Ok(())
     }
 
     fn eval_top_level(&mut self, top_level: &TopLevel) -> Result<()> {
-        self.eval_top_level_with_context(top_level, None)
+        Ok(()) /* ~ changed by cargo-mutants ~ */
     }
 
     fn eval_top_level_with_context(
         &mut self,
         top_level: &TopLevel,
         current_module: Option<&str>,
     ) -> Result<()> {
         match top_level {
