--- crates/jtv-core/src/formatter.rs
+++ replace += with *= in Formatter::format_module
@@ -75,17 +75,17 @@
             }
         }
     }
 
     fn format_module(&mut self, module: &ModuleDecl) {
         self.write_indent();
         self.output
             .push_str(&format!("module {} {{\n", module.name));
-        self.indent_level += 1;
+        self.indent_level *= /* ~ changed by cargo-mutants ~ */ 1;
 
         for stmt in &module.body {
             self.format_top_level(stmt);
         }
 
         self.indent_level -= 1;
         self.write_indent();
         self.output.push_str("}\n");
