--- crates/jtv-core/src/parser.rs
+++ replace == with != in parse_factor
@@ -368,17 +368,17 @@
                 elements.push(parse_data_expr(elem)?);
             }
             Ok(DataExpr::Tuple(elements))
         }
         Rule::factor => {
             let mut inner = pair.into_inner();
             let first = inner.next().unwrap();
 
-            if first.as_rule() == Rule::unary_op {
+            if first.as_rule() != /* ~ changed by cargo-mutants ~ */ Rule::unary_op {
                 let op = first.as_str();
                 let expr = parse_factor(inner.next().unwrap())?;
 
                 match op {
                     "-" => Ok(DataExpr::Negate(Box::new(expr))),
                     _ => Err(JtvError::ParseError(format!(
                         "Unknown unary operator: {}",
                         op
