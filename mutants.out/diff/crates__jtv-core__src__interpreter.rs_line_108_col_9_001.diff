--- crates/jtv-core/src/interpreter.rs
+++ replace Interpreter::get_last_result -> Option<&Value> with Some(Box::leak(Box::new(Default::default())))
@@ -100,17 +100,17 @@
     pub fn get_variables(&self) -> Vec<(String, Value)> {
         self.globals
             .iter()
             .map(|(k, v)| (k.clone(), v.clone()))
             .collect()
     }
 
     pub fn get_last_result(&self) -> Option<&Value> {
-        self.last_result.as_ref()
+        Some(Box::leak(Box::new(Default::default()))) /* ~ changed by cargo-mutants ~ */
     }
 
     fn add_trace(&mut self, stmt_type: &str, line: &str) {
         if self.trace_enabled {
             let env: HashMap<String, String> = self
                 .globals
                 .iter()
                 .map(|(k, v)| (k.clone(), format!("{}", v)))
